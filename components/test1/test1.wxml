<!--components/test1/test1.wxml-->

<view>
<picker mode="selector" range="{{name}}" class="picker" value="{{index}}" bindchange="ChangeName"  name="{{name[index]}}">
<view class="choose">{{name[index]}}</view>
</picker>
<picker  mode="selector" range="{{number}}" class="picker" value="{{anotherindex}}" bindchange="ChangeNums" name="{{number[anotherindex]}}">
<view class="number">{{number[anotherindex]}}</view>
</picker>
<view class="kucun">{{kucun[index]}}</view>
</view>
